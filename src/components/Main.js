import React from 'react';
import Layout from './Layout';
import MidSection from './MidSection';

const Main = (props) => {
  return <div>
      <Layout>
          <MidSection>
              
          </MidSection>
      </Layout>
  </div>;
}

export default Main;
